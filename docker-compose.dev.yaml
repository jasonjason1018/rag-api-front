services:

  frontend:
    build:
      context: .
      target: builder
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
    extra_hosts:
      - "host.docker.internal:host-gateway"
